{
  "name": "swap-attack",
  "version": "0.1.0",
  "description": "Browser-based tile-swapping puzzle game (Tetris Attack / Panel de Pon clone)",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
